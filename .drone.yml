kind: pipeline
name: amd

platform:
  os: linux
  arch: amd64

steps:
- name: opam
  image: ocaml/opam2
  commands:
  - sudo chown -R opam .
  - git -C /home/opam/opam-repository pull origin
  - opam repo add overlays .
  - opam update -uy
  - env OPAMJOBS=40 opam --yes depext -iy `./list-overrides.sh`
- name: duniverse
  image: ocaml/opam2
  commands:
  - sudo chown -R opam .
  - git -C /home/opam/opam-repository pull origin
  - export OPAMJOBS=40
  - opam repo add overlays .
  - opam update -uy
  - ./list-overrides.sh > pkglist.txt
  - opam pin add -n duniverse https://github.com/avsm/duniverse.git
  - opam --yes depext -iy duniverse
  - mkdir duniverse-build
  - cd duniverse-build
  - opam exec -- duniverse init `cat ../pkglist.txt`
  - opam exec -- duniverse pull
  - opam depext -y `cat ../pkglist.txt`
  - opam install -y dune
  - opam exec -- dune build
- name: outofdate
  image: ocaml/opam2
  commands:
  - sudo chown -R opam .
  - git -C /home/opam/opam-repository pull origin
  - opam repo add overlays .
  - opam update -uy
  - ./check-upstream.sh
- name: all
  image: ocaml/opam2
  depends_on:
  - outofdate
  - duniverse
  - opam
